# RabbitMQ_celery_django
This is a simple django project, where rabbitmq and celery is used, there is a doc which guide you to how install rabbitmq in windows os, and simple example to show celery working.
